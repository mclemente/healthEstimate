    <div id="{{id}}" class="{{classes}}" data-appid="{{appId}}">
    <span class="estimation" style="text-shadow: 0 0 10px {{status.colorShadow}} ; color: {{status.color}}">
        {{status.desc}}
    </span>
</div>
